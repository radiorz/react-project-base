/**
 * @author
 * @file the-bar.tsx
 * @fileBase the-bar
 * @path packages\react-ui\lib\the-bar\the-bar.tsx
 * @from
 * @desc
 * @example
 */

import { useState, useEffect, memo } from "react";
interface Props {
  // value: propTypes.any
}
export const TheBar: React.FC<Props> = ({
  renderTitle,
  actions,
  handleActions,
}) => {
  return <div>TheBar</div>;
};
